spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/school_fees}
    username: ${DB_USERNAME:school}
    password: ${DB_PASSWORD:school_dev_password}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
server:
  error:
    include-message: never

